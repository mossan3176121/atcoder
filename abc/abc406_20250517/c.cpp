#include <bits/stdc++.h>
typedef long long ll;

int main()
{
    int n;
    std::cin >> n;
    std::vector<ll> p(n);
    for (int i = 0; i < n; i++)
    {
        std::cin >> p[i];
    }
    
    
    return 0;
}